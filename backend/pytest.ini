[pytest]
markers =
    slow: marks tests as slow (LLM calls, deselect with '-m "not slow"')
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
